{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/joshuaslate/reactalytics",
  "devDependencies": {
    "@testing-library/react": "^13.4.0",
    "@types/jest": "^29.2.3",
    "@types/node": "^18.11.9",
    "jest": "^29.3.1",
    "jest-environment-jsdom": "^29.3.1",
    "lerna": "^6.0.3",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "clean": "lerna run clean",
    "build": "yarn clean && lerna run build",
    "pub": "yarn build && lerna publish",
    "test": "jest"
  }
}
