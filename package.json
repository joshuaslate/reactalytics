{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "repository": "https://github.com/joshuaslate/reactalytics",
  "devDependencies": {
    "@testing-library/react": "^14.0.0",
    "@types/jest": "^29.2.3",
    "@types/node": "^18.11.9",
    "jest": "^29.3.1",
    "jest-environment-jsdom": "^29.3.1",
    "lerna": "^6.0.3",
    "prettier": "^3.0.0",
    "ts-jest": "^29.0.3",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "clean": "lerna run clean",
    "build": "yarn clean && lerna run build",
    "pub": "yarn build && lerna publish --no-private",
    "test": "jest"
  }
}
